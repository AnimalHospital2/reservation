server:
  port: 8081
spring:
#  data:
#    rest:
#      basePath: /space/api
  profiles: default
  cloud:
    stream:
      kafka:
        binder:
          brokers: localhost:9092
      bindings:
        input:
          group: reservation
          destination: animal
          contentType: application/json
        output:
          destination: animal
          contentType: application/json
---
server:
  port: 8080

spring:
  profiles: docker
#  data:
#    rest:
#      basePath: /space/api
  cloud:
    stream:
      kafka:
        binder:
          brokers: my-kafka.kafka.svc.cluster.local:9092
      bindings:
        input:
          group: reservation
          destination: animal
          contentType: application/json
        output:
          destination: animal
          contentType: application/json